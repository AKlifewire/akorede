import 'package:flutter/material.dart';
import 'app_web.dart';

// This is a simplified version of the app for web platforms
// that doesn't rely on Amplify or other plugins with web compatibility issues

void main() {
  runApp(const WireAppWeb());
}